const express = require("express");
const router = express.Router();

router.get("/", () => {});
router.post("/", () => {});
router.patch("/", () => {});
router.delete("/", () => {});
module.exports = router;

// Agregar populete de Mongoose para data de organizador
